{
  "name": "@studyforge/rag-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc",
    "lint": "echo 'noop' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'noop' && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@studyforge/shared-types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": { "typescript": "^5.6.2" }
}
