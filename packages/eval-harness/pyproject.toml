[project]
name = "studyforge-eval-harness"
version = "0.1.0"
description = "Ragas + Promptfoo + golden-set evaluation harness for StudyForge AI."
requires-python = ">=3.11"
dependencies = [
  "ragas>=0.2.0",
  "datasets>=3.0.0",
  "pydantic>=2.9.0",
  "pytest>=8.3.3",
]
