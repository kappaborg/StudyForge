# @studyforge/eval-harness

Ragas (faithfulness, context precision/recall, answer relevancy) + Promptfoo regression suites.

Targets enforced in CI:

| Metric | Threshold |
| --- | --- |
| Ragas faithfulness | ≥ 0.85 |
| Ragas context precision | ≥ 0.80 |
| Quiz rationale-consistency (custom) | ≥ 0.95 |

Implementation lands in Phase 1 alongside the tutor MVP.
