# Dev overlay — kind / k3d. Lightweight resources, debug logging, no TLS.

replicas:
  web: 1
  api: 1
  aiWorker: 1

env:
  NODE_ENV: development
  LOG_LEVEL: debug

ingress:
  tls:
    enabled: false

serviceMonitor:
  enabled: false
