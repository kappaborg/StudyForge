{
  "name": "sandbox-runner",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8003",
    "start": "uv run uvicorn src.main:app --host 0.0.0.0 --port 8003",
    "build": "echo 'sandbox-runner has no JS build step' && exit 0",
    "lint": "echo 'sandbox-runner lint runs in the python ci job' && exit 0",
    "typecheck": "echo 'sandbox-runner typecheck runs in the python ci job' && exit 0",
    "test": "echo 'sandbox-runner test runs in the python ci job' && exit 0",
    "clean": "rm -rf .ruff_cache .pytest_cache .mypy_cache"
  }
}
