{
  "name": "ai-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8001",
    "start": "uv run uvicorn src.main:app --host 0.0.0.0 --port 8001",
    "lint": "echo 'ai-worker lint runs in the python ci job' && exit 0",
    "typecheck": "echo 'ai-worker typecheck runs in the python ci job' && exit 0",
    "test": "echo 'ai-worker test runs in the python ci job' && exit 0",
    "clean": "rm -rf .ruff_cache .pytest_cache .mypy_cache"
  }
}
